#include <stdio.h>
#include <stdlib.h>

int main()
{
    int amount =0;
    while (amount<137)
    {
        printf("Hello, world!\n");
        ++amount;
    }
    printf("Total amount is %d", amount);
    return 0;
}
